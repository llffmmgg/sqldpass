# payment

결제 관련 전용 코드가 모이는 모듈.

## 구성

_(구조 확정 전 — 필요에 따라 채워 넣을 것)_

## 다른 모듈과의 관계

- `backend/` — 결제 검증·주문 상태 업데이트가 필요하면 backend 쪽 API와 연동
- `frontend/` — 결제 UI 진입은 `frontend/src/app/` 내 라우트에서 이 모듈의 SDK/공개 인터페이스 호출

## 로컬 실행

추후 작성.
