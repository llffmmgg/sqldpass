NEXT_PUBLIC_API_URL=http://localhost:8080
# AdSense 오른쪽 사이드 300x600 광고 슬롯 ID (비워두면 광고 미렌더)
NEXT_PUBLIC_ADSENSE_SIDEBAR_SLOT=

# PortOne V2 — 카드사 심사용 결제 페이지 (/checkout) 에서 사용.
# 둘 다 비어있으면 결제창 호출 자체가 실패한다.
# store-{uuid} : PortOne 어드민 → 상점 → Store ID
# channel-key  : PortOne 어드민 → 결제 연동 → 채널 → 채널 키 (한 PG 사 한 곳)
NEXT_PUBLIC_PORTONE_STORE_ID=
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=
